# Generated requirements for UAV-TIR-VISIBLE-FUSION
# Core Python packages used by the project
numpy
opencv-python
tifffile
Pillow
PyYAML
matplotlib
h5py

# Notes:
# - Agisoft Metashape Python API (`Metashape`) must be installed via
#   Agisoft/Metashape installer; it is not provided through PyPI.
# - ExifTool (external binary) and DJI SDK are external requirements
#   referenced in docs and must be installed separately.