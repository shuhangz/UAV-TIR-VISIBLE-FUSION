# 合并自: ./requirements.txt, ./1-3/requirements.txt, ./6-9/requirements.txt
# 生成日期: 2026-05-10

# ==================================================
# 核心依赖（已合并、去重，选择兼容的较新版本）
# ==================================================

opencv-python>=4.8.0        # 图像读写、特征检测、几何变换、滤波
numpy>=1.24.0               # 数组计算、矩阵运算、线性代数
scipy>=1.17.0               # 科学计算依赖（TWMM 运行时需要）
pandas>=3.0.0               # 数据处理（TWMM 运行时需要）
torch>=2.11.0               # 深度学习依赖（TWMM 运行时需要）
joblib>=1.5.0               # 并行/缓存

# 图形/可视化 与 I/O
matplotlib>=3.5.0
h5py>=3.6.0
tifffile
Pillow
PyYAML>=6.0
rasterio>=1.4.3

# ==================================================
# 备注与安装说明
# ==================================================
# - 本文件为三处 requirements 的合并结果，保留了较新的版本约束以提高兼容性。
# - Agisoft Metashape Professional 2.2.1 的 Python API 需单独安装（无法通过公开 pip 获取），
#   若使用重建/富集模块，请参见 docs 与 metashape_python_api_2_2_1_MinerU__20251017023519.md。
# - TWMM-main（仓库内）通过 sys.path 注入，不需要 pip 安装，但可能需要满足其运行时依赖（torch/scipy/pandas）。
# - 推荐 Python >= 3.8（建议 3.9+）。

# 开发/测试（可选）:
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# pylint>=2.15.0
# mypy>=0.990

# 安装示例：
# python -m venv venv
# venv\Scripts\activate  # Windows
# pip install -r requirements.txt

